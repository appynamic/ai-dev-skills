# Installed by the journal Claude Code plugin (https://github.com/appynamic/ai-dev-skills).
# Actual logic lives in .claude/journal-git-hook.mjs (project-committed, self-contained).
node "$(git rev-parse --show-toplevel)/.claude/journal-git-hook.mjs" "$@"
